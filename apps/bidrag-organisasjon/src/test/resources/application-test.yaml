ARBEIDSFORDELING_URL: http://localhost:${wiremock.server.port:123}/norg2
ENTRA_PROXY_URL: http://localhost:${wiremock.server.port:123}/entra
TILGANGSKONTROLL_URL: http://localhost:${wiremock.server.port:123}/tilgang
SKJERMING_URL: http://localhost:${wiremock.server.port:123}/skjerming
PERSON_URL: http://localhost:${wiremock.server.port:123}/person

SRVBIDRAG_USERNAME: usr
SRVBIDRAG_PASSWORD: psw
no.nav.security.jwt:
  client:
    registration:
      person:
        token-endpoint-url: http://localhost:${mock-oauth2-server.port:1234}/aad/token
        resource-url: ${PERSON_URL}
        grant-type: urn:ietf:params:oauth:grant-type:jwt-bearer
        scope: api://123/.default
        authentication:
          client-id: 123
          client-secret: 123
          client-auth-method: client_secret_basic
  issuer.aad:
    discovery_url: http://localhost:${mock-oauth2-server.port}/aad/.well-known/openid-configuration
    accepted_audience: aud-localhost

LDAP_URL: ldaps://ldapgw.test.local
LDAP_PASSWORD: CHANGEME
LDAP_DOMAIN: TEST.LOCAL
LDAP_USERNAME: srvSSOLinux
LDAP_BASEDN: dc=test,dc=local

NAIS_APP_NAME: bidrag-organisajon

cache:
  warming:
    enabled: false

