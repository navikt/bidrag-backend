name: Bidrag-reisekostnad-api
on:
  workflow_call:
    inputs:
      bibliotekartefakt:
        description: Navn på artefaktet med ferdige biblioteker fra bygg-apper.
        required: false
        type: string
        default: ''
      miljo:
        required: false
        type: string
        default: ''
      hopp_over_tester:
        required: false
        type: boolean
        default: false
  workflow_dispatch:
    inputs:
      miljo:
        description: "Miljø som skal deployes til"
        required: true
        type: choice
        options:
          - prod
          - q1
      hopp_over_tester:
        description: >-
          Hopp over tester og ktlint for å tvinge gjennom en deploy.
          Fungerer kun for q1/q2 - ignoreres for prod.
        required: false
        type: boolean
        default: false

env:
  APP_PATHS: |
    pom.xml
    libs/bidrag-felles/**
    .nais/bidrag-reisekostnad-api/**.yaml
    apps/bidrag-reisekostnad-api/**

jobs:
  bygg_test_og_deploy:
    name: "Bygg, test og deploy"
    permissions:
      contents: "write"
      id-token: "write"
    uses: ./.github/workflows/bygg_og_deploy.yaml
    secrets: inherit
    with:
      bibliotekartefakt: ${{ inputs.bibliotekartefakt }}
      runner: ubuntu-latest-4-cores
      nais_hovedfil_navn: bidrag-reisekostnad-api/nais.yaml
      nais_variabler_filnavn_q1: bidrag-reisekostnad-api/preprod.yaml
      deploy_q1: ${{ github.event_name == 'workflow_dispatch' && inputs.miljo == 'q1' }}
      nais_variabler_filnavn_prod: bidrag-reisekostnad-api/prod.yaml
      deploy_prod: ${{ (github.event_name == 'workflow_dispatch' && inputs.miljo == 'prod') || (github.event_name == 'push' && github.ref == 'refs/heads/main') }}
      skip_tester: ${{ inputs.hopp_over_tester == true && (inputs.miljo == 'q1' || inputs.miljo == 'q2') }}
      maven_options: -B -fae -pl apps/bidrag-reisekostnad-api
      maven_cache_paths: |
        pom.xml
        apps/bidrag-reisekostnad-api/pom.xml
      ktlint_paths: apps/bidrag-reisekostnad-api/src/**/*.kt
      docker_context: ./apps/bidrag-reisekostnad-api
      dockerfile_with_path: ./apps/bidrag-reisekostnad-api/Dockerfile
      image_suffix: bidrag-reisekostnad-api
      tag: bidrag-reisekostnad-api
