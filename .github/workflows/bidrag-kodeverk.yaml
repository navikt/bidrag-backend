name: Bidrag-kodeverk
on:
  workflow_dispatch:
    inputs:
      miljo:
        description: "Miljø som skal deployes til"
        required: true
        type: choice
        options:
          - prod
          - q1
  pull_request:
    types: [labeled, unlabeled]
    paths:
      - '.nais/bidrag-kodeverk/**.yaml'
      - 'apps/bidrag-kodeverk/**'
  push:
    branches:
      - '**'
      - '!dependabot/**'
    paths:
      - '.nais/bidrag-kodeverk/**.yaml'
      - 'apps/bidrag-kodeverk/**'

jobs:
  detect_changes:
    name: "Sjekk om appen faktisk er endret"
    runs-on: ubuntu-latest
    permissions:
      contents: "read"
    outputs:
      changed: ${{ steps.detect.outputs.changed }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - uses: ./.github/actions/utled-app-endringer
        id: detect

  finn_miljo:
    name: "Bestem hvilke miljø som skal deployes"
    needs: detect_changes
    if: needs.detect_changes.outputs.changed == 'true' && github.actor != 'dependabot[bot]'
    runs-on: ubuntu-latest
    permissions:
      contents: "read"
      pull-requests: "read"
    outputs:
      deploy_q1: ${{ steps.finn.outputs.deploy_q1 }}
      deploy_prod: ${{ steps.finn.outputs.deploy_prod }}
    steps:
      - uses: ./.github/actions/finn-deploy-miljo
        id: finn
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          dispatch-miljo: ${{ inputs.miljo }}

  bygg_test_og_deploy:
    name: "Bygg, test og deploy"
    needs: finn_miljo
    permissions:
      contents: "write"
      id-token: "write"
    uses: ./.github/workflows/bygg_og_deploy.yaml
    secrets: inherit
    with:
      nais_hovedfil_navn: bidrag-kodeverk/nais.yaml
      nais_variabler_filnavn_q1: bidrag-kodeverk/dev.yaml
      deploy_q1: ${{ needs.finn_miljo.outputs.deploy_q1 == 'true' }}
      nais_variabler_filnavn_prod: bidrag-kodeverk/prod.yaml
      deploy_prod: ${{ needs.finn_miljo.outputs.deploy_prod == 'true' }}
      maven_options: -B -fae -pl apps/bidrag-kodeverk -am
      maven_cache_paths: |
        pom.xml
        apps/bidrag-kodeverk/pom.xml
      ktlint_paths: apps/bidrag-kodeverk/src/**/*.kt
      docker_context: ./apps/bidrag-kodeverk
      image_suffix: bidrag-kodeverk
      tag: bidrag-kodeverk
