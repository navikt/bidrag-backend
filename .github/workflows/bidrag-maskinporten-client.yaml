name: Bidrag-maskinporten-client
on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Miljø som skal deployes til"
        required: true
        type: choice
        options:
          - dev
          - prod
  push:
    branches:
      - '**'
      - '!dependabot/**'
    paths:
      - '.nais/bidrag-maskinporten-client/**.yaml'
#      - '.github/workflows/bidrag-maskinporten-client.yaml' //TODO(Slå på etter migrering)
#     - 'pom.xml' //TODO(Slå på etter migrering)
      - 'apps/bidrag-maskinporten-client/**'

jobs:
  detect_changes:
    name: "Sjekk om appen faktisk er endret"
    runs-on: ubuntu-latest
    permissions:
      contents: "read"
    outputs:
      changed: ${{ steps.detect.outputs.changed }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - uses: ./.github/actions/utled-app-endringer
        id: detect

  bygg_og_test:
    needs: detect_changes
    if: needs.detect_changes.outputs.changed == 'true' && (github.event_name == 'push' && github.actor != 'dependabot[bot]' && github.ref != 'refs/heads/main')
    name: "Bygg og test"
    runs-on: ubuntu-latest
    permissions:
      contents: "read"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: temurin
          java-version: '21'
      # Egen, smal cache-nøkkel (kun denne appens pom-kjede) istedenfor setup-java sin
      # innebygde `cache: maven`, som hasher **alle** pom.xml i monorepoet og dermed
      # invalideres av endringer i moduler denne jobben ikke bygger.
      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.m2/repository
          key: maven-bidrag-maskinporten-client-${{ hashFiles('pom.xml', 'apps/bidrag-maskinporten-client/pom.xml') }}
          restore-keys: |
            maven-bidrag-maskinporten-client-
      - uses: s4u/maven-settings-action@894661b3ddae382f1ae8edbeab60987e08cf0788 # v4.0.0
        with:
          servers: |
            [
              {
                "id": "github",
                "username": "x-access-token",
                "password": "${{ secrets.READER_TOKEN }}"
              }
            ]
      - name: Bygg og kjør tester (inkl. ktlint)
        run: mvn -B -fae verify -pl apps/bidrag-maskinporten-client -am

  deploy_dev:
    needs: detect_changes
    if: needs.detect_changes.outputs.changed == 'true' && ((github.event_name == 'push' && github.ref != 'refs/heads/main') || (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'dev'))
    name: "Build and deploy to dev"
    concurrency:
      group: deploy-maskinporten-client-dev-${{ github.ref }}
      cancel-in-progress: true
    permissions:
      contents: "read"
      id-token: "write"
    uses: ./.github/workflows/bygg_og_deploy.yaml
    secrets: inherit
    with:
      maven_options: -B -fae -pl apps/bidrag-maskinporten-client -am
      maven_cache_paths: |
        pom.xml
        apps/bidrag-maskinporten-client/pom.xml
      ktlint_paths: apps/bidrag-maskinporten-client/src/**/*.kt
      docker_context: ./apps/bidrag-maskinporten-client
      nais_variabler_filnavn: bidrag-maskinporten-client/dev.yaml
      nais_hovedfil_navn: bidrag-maskinporten-client/nais.yaml
      image_suffix: bidrag-maskinporten-client
      tag: bidrag-maskinporten-client

  deploy_prod:
    needs: detect_changes
    if: needs.detect_changes.outputs.changed == 'true' && ((github.event_name == 'push' && github.ref == 'refs/heads/main') || (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'prod'))
    name: "Build and deploy to prod"
    concurrency:
      group: deploy-maskinporten-client-prod-${{ github.ref }}
      cancel-in-progress: true
    permissions:
      contents: "write"
      id-token: "write"
    uses: ./.github/workflows/bygg_og_deploy_prod.yaml
    secrets: inherit
    with:
      maven_options: -B -fae -pl apps/bidrag-maskinporten-client -am
      maven_cache_paths: |
        pom.xml
        apps/bidrag-maskinporten-client/pom.xml
      ktlint_paths: apps/bidrag-maskinporten-client/src/**/*.kt
      docker_context: ./apps/bidrag-maskinporten-client
      nais_variabler_filnavn: bidrag-maskinporten-client/prod.yaml
      nais_hovedfil_navn: bidrag-maskinporten-client/nais.yaml
      image_suffix: bidrag-maskinporten-client
      tag: bidrag-maskinporten-client
