name: Bidrag-aktørregister
on:
  workflow_dispatch:
    inputs:
      miljo:
        description: "Miljø som skal deployes til"
        required: true
        type: choice
        options:
          - prod
          - q1
          - q2
  pull_request:
    types: [labeled, unlabeled]
    paths:
      - '.nais/bidrag-aktoerregister/**.yaml'
      - 'apps/bidrag-aktoerregister/**'
  push:
    branches:
      - '**'
      - '!dependabot/**'
    paths:
      - '.nais/bidrag-aktoerregister/**.yaml'
      - 'apps/bidrag-aktoerregister/**'

jobs:
  detect_changes:
    name: "Sjekk om appen faktisk er endret"
    runs-on: ubuntu-latest
    permissions:
      contents: "read"
    outputs:
      changed: ${{ steps.detect.outputs.changed }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - uses: ./.github/actions/utled-app-endringer
        id: detect

  finn_miljo:
    name: "Bestem hvilke miljø som skal deployes"
    needs: detect_changes
    if: needs.detect_changes.outputs.changed == 'true' && github.actor != 'dependabot[bot]'
    runs-on: ubuntu-latest
    permissions:
      contents: "read"
      pull-requests: "read"
    outputs:
      deploy_q1: ${{ steps.finn.outputs.deploy_q1 }}
      deploy_q2: ${{ steps.finn.outputs.deploy_q2 }}
      deploy_prod: ${{ steps.finn.outputs.deploy_prod }}
    steps:
      - uses: ./.github/actions/finn-deploy-miljo
        id: finn
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          dispatch-miljo: ${{ inputs.miljo }}

  bygg_test_og_deploy:
    name: "Bygg, test og deploy"
    needs: finn_miljo
    permissions:
      contents: "write"
      id-token: "write"
    uses: ./.github/workflows/bygg_og_deploy.yaml
    secrets: inherit
    with:
      nais_hovedfil_navn: bidrag-aktoerregister/nais.yaml
      nais_variabler_filnavn_q1: bidrag-aktoerregister/q1.yaml
      deploy_q1: ${{ needs.finn_miljo.outputs.deploy_q1 == 'true' }}
      nais_variabler_filnavn_q2: bidrag-aktoerregister/q2.yaml
      deploy_q2: ${{ needs.finn_miljo.outputs.deploy_q2 == 'true' }}
      nais_variabler_filnavn_prod: bidrag-aktoerregister/prod.yaml
      deploy_prod: ${{ needs.finn_miljo.outputs.deploy_prod == 'true' }}
      maven_options: -B -fae -pl apps/bidrag-aktoerregister -am
      maven_cache_paths: |
        pom.xml
        apps/bidrag-aktoerregister/pom.xml
      ktlint_paths: apps/bidrag-aktoerregister/src/**/*.kt
      docker_context: ./apps/bidrag-aktoerregister
      image_suffix: bidrag-aktoerregister
      tag: bidrag-aktoerregister
