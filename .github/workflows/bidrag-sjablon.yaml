name: Bidrag-sjablon
on:
  workflow_call:
    inputs:
      bibliotekartefakt:
        description: Navn på artefaktet med ferdige biblioteker fra bygg-apper.
        required: false
        type: string
        default: ''
      miljo:
        required: false
        type: string
        default: ''
      hopp_over_tester:
        required: false
        type: boolean
        default: false
  workflow_dispatch:
    inputs:
      miljo:
        description: "Miljø som skal deployes til"
        required: true
        type: choice
        options:
          - prod
          - q1
      hopp_over_tester:
        description: >-
          Hopp over tester og ktlint for å tvinge gjennom en deploy.
          Fungerer kun for q1/q2 - ignoreres for prod.
        required: false
        type: boolean
        default: false

env:
  APP_PATHS: |
    pom.xml
    .nais/bidrag-sjablon/**.yaml
    apps/bidrag-sjablon/**

jobs:
  bygg_test_og_deploy:
    name: "Bygg, test og deploy"
    permissions:
      contents: "write"
      id-token: "write"
    uses: ./.github/workflows/bygg_og_deploy.yaml
    secrets: inherit
    with:
      bibliotekartefakt: ${{ inputs.bibliotekartefakt }}
      bibliotekgrupper: ''
      nais_cluster: fss
      nais_hovedfil_navn: bidrag-sjablon/nais.yaml
      nais_variabler_filnavn_q1: bidrag-sjablon/dev.yaml
      deploy_q1: ${{ github.event_name == 'workflow_dispatch' && inputs.miljo == 'q1' }}
      nais_variabler_filnavn_prod: bidrag-sjablon/prod.yaml
      deploy_prod: ${{ (github.event_name == 'workflow_dispatch' && inputs.miljo == 'prod') || (github.event_name == 'push' && github.ref == 'refs/heads/main') }}
      skip_tester: ${{ inputs.hopp_over_tester == true && (inputs.miljo == 'q1' || inputs.miljo == 'q2') }}
      maven_options: -B -fae -pl apps/bidrag-sjablon
      maven_cache_paths: |
        pom.xml
        apps/bidrag-sjablon/pom.xml
      ktlint_paths: apps/bidrag-sjablon/src/**/*.kt
      docker_context: ./apps/bidrag-sjablon
      dockerfile_with_path: ./apps/bidrag-sjablon/Dockerfile
      image_suffix: bidrag-sjablon
      tag: bidrag-sjablon
